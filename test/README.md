# Тесты

```bash
npm test           # разовый прогон
npm run test:watch # watch
npm run typecheck  # tsc -noEmit (только .ts, .vue он не проверяет)
```

Раннер - vitest 2 + jsdom + @vue/test-utils. Версии подобраны под Node 18:
vite 5 и vitest 2, выше требуют Node 20+. Установка делалась с
`--legacy-peer-deps` из-за `@types/node` в devDependencies.

## Что где

| Файл | Назначение |
|---|---|
| `mocks/obsidian.ts` | Мок API Obsidian: Vault с файлами, событиями и `process`, Plugin, ItemView, Setting, Notice, MarkdownRenderer, debounce |
| `helpers.ts` | Фиксированные часы (13.08.2026), сборка текста файла задачи и плагин-двойник с картой |
| `setup.ts` | Полифил `Element.empty()` - в Obsidian его даёт приложение |
| `taskFormat.test.ts` | Формат: грамматика повтора, расчёт следующей даты, разбор блоков, тело, ключ сортировки, имя файла, правки блока |
| `taskMap.test.ts` | Карта: индексация, автоматика чекбокса, генерация повтора, перенос даты, создание задачи, события хранилища |
| `TaskCalendar.test.ts` | Календарь: сетка, навигация, точки, группы и порядок карточек, кнопки, поле создания |
| `TaskBody.test.ts` | Рендер тела: markdown, ссылки, чекбоксы, предпросмотр по наведению |
| `main.test.ts` | Настройки, onload, openCalendar, жизненный цикл view, подъём и остановка карты |

Пакет `obsidian` содержит только `.d.ts`, рантайма у него нет - поэтому в
`vitest.config.mts` он и bare-импорт `main` подменяются алиасами.

## Соглашения

- Время фиксировано: `useFixedClock()` подменяет только `Date`, `setTimeout` и
  `clearTimeout`. `setImmediate` должен остаться настоящим, иначе `flushPromises`
  из @vue/test-utils зависает.
- Сегодня в тестах - четверг 13.08.2026, сетка августа стартует с 27 июля.
- Автоматика чекбокса срабатывает только на **переключение**, поэтому тест сперва
  поднимает карту (`map.start()` или монтирование календаря), а уже потом что-то
  отмечает. Отметка на первой же индексации - это «сделано при выключенном
  плагине», и файл не правится.
- События хранилища идут через debounce 300 мс: после `vault.modify` нужен
  `vi.advanceTimersByTimeAsync(300)`. Своя же запись карты сводится сразу, ждать
  не нужно.
- Текст файла задачи собирается `taskFileText()` из `helpers.ts` - не пишите
  эмодзи параметров руками, иначе тест поедет вместе с опечаткой.
- Тесты с префиксом **ЗАФИКСИРОВАН ДЕФЕКТ** закрепляют текущее *неверное*
  поведение, чтобы починка была заметна: такой тест упадёт, когда баг исправят,
  и его надо будет переписать на ожидаемое поведение.
